# Port the Next.js server listens on inside the container
PORT=3000

# Directory where account/transaction/goal data is persisted as JSON.
# Mount a Dokploy volume at this path so data survives redeploys.
DATA_DIR=/app/data
